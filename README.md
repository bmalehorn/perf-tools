Busybox-compatible perf-tools
=============================

`perf-tools`, ported to basic shell scripts, so it works on both `bash`
and `ash` (from busybox).

Currently supported:

[x] execsnoop




